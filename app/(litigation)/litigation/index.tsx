import { SoftSkyBackdrop } from "@/components/backgrounds/SoftSkyBackdrop";
import { LitigationCaseBuilder } from "@/modules/litigation/components/LitigationCaseBuilder";

export default function LitigationCaseBuilderPage() {
  return (
    <SoftSkyBackdrop>
      <LitigationCaseBuilder />
    </SoftSkyBackdrop>
  );
}
